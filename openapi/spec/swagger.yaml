openapi: 3.0.0
components:
  examples: {}
  headers: {}
  parameters: {}
  requestBodies: {}
  responses: {}
  schemas:
    HealthResponse:
      properties:
        status:
          type: string
          enum:
            - ok
          nullable: false
        timestamp:
          type: string
        uptime:
          type: number
          format: double
      required:
        - status
        - timestamp
        - uptime
      type: object
      additionalProperties: false
  securitySchemes: {}
info:
  title: backend-node
  version: 1.0.0
  license:
    name: ISC
  contact: {}
paths:
  /health:
    get:
      operationId: GetHealth
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
      tags:
        - Health
      security: []
      parameters: []
servers:
  - url: /
